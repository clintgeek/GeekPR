[pytest]
# Add the backend root (where the `app` package lives) to sys.path so
# `from app.services... import ...` works both in CI and from a local
# checkout without needing to export PYTHONPATH=. first.
pythonpath = .
testpaths = tests
